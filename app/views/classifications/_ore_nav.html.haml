
%ul.ore-nav
  
  - for ore in Ore.all

    %li
      = link_to image_tag(ore.tile) + "#{ore} (#{Item.item_count(classification, ore)})", armory_items_path(classification, :ore => ore.to_param), :title => ore, :class => "ore-icon"