#forge

  #sidebar
    %h3= @forge

    .progress-wrap
      = render "forges/progress", :forge => @forge
    
    #event-list

      - @forge.events.reverse.take(20).each do |event|
        = render "events/#{event.type}", :event => event

  - if @forge.complete? and @forge.requires_funding?
    #complete
      (Placeholder) This forge is complete
  - else
  
    #ores


  = render "active_forge"

:javascript
  ForgeCraft.Config.forge = #{@forge.to_json}
  ForgeCraft.Config.ores = #{Ore.tile_urls.to_json}
  ForgeCraft.Config.loot = #{@loot.to_json}
  ForgeCraft.Config.perfect_accuracy = #{Forge::PERFECT_ACCURACY}
  ForgeCraft.Config.unlock_accuracy = #{Forge::UNLOCK_ACCURACY}
