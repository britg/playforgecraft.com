#active-forge.active-forge
  
  #bar-container
    
    #bar

  - 1.times do |i|
    .checkpoint{ :id => "checkpoint-#{i}" }
      .marker
      = link_to image_tag("forge_checkpoint.png"), "#"

  .instructions Press spacebar to forge