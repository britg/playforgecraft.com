!!!
%html
  %head
    %meta{'http-equiv' => "content-type", :content => "text/html;charset=UTF-8"}
    %title= @browser_title || 'Forgecraft - Epic Loot, Anywhere'

    = render "shared/mobile_meta"
    = render "shared/favicon"

    = stylesheet_link_tag "home"
    = csrf_meta_tags

    = javascript_include_tag "home"

    = render "shared/console" if Rails.env.production?
    = render "shared/ga" if Rails.env.production?
    
  %body{ :class => params[:controller] }

    .topbar
      .container

        = render "shared/logo"

        = link_to "Existing players, log in &rarr;".html_safe, login_path, :class => "log-in-link"

    #content
    
      = yield